import * as React from "react";
import { ReturnPolicy as ReturnPolicyComponent } from "@/components";

export const dynamic = "force-dynamic";

export default function ReturnPolicy() {
  return <ReturnPolicyComponent />;
}
